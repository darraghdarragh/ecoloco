import React from 'react';

function Header() {
  return (
    <header>
      <h1>Eco Community</h1>
      {/* Add navigation, user profile, or other header components as needed */}
    </header>
  );
}

export default Header;
